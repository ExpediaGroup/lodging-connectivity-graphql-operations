fragment PaginatedReservationsSummaries on ReservationsConnection {
    totalCount
    pageInfo {
        hasNextPage
        endCursor
    }
    edges {
        cursor
        node {
            ...ReservationSummaryData
        }
    }
}
