fragment PaginatedReservationsData on ReservationsConnection {
    totalCount
    pageInfo {
        hasNextPage
        endCursor
    }
    edges {
        cursor
        node {
            ...ReservationData
        }
    }
}
