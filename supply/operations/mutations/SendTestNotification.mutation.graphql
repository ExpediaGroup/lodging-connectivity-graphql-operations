mutation SendTestNotification($input: SendTestNotificationInput!) {
    sendTestNotification(input: $input) {
        httpStatusCode
        outcome
        error {
            code
            message
        }
    }
}
