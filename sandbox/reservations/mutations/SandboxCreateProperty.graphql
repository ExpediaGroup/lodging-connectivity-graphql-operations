mutation SandboxCreateProperty($name: String, $clientMutationId: String) {
    createProperty(input: { name: $name, clientMutationId: $clientMutationId }) {
        clientMutationId
        property {
            id
            name
        }
    }
}
